# Emoji-Localisation

This is a simple project which predicts the locations of an emoji in an image. It then gives bounding box coordinates and the label predicted which is then used to draw a bounding box around the emoji in the image and display the label. 
